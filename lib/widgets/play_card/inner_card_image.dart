import 'package:flutter/material.dart';

class InnerCardImage extends StatelessWidget {
  const InnerCardImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
